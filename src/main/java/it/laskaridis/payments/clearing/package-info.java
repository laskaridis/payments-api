/**
 * Contains classes implementing the credit card clearing
 * business domain functionality (i.e. controllers, model,
 * and view objects).
 *
 * Classes are further grouped depending on their functional
 * purpose in any of the following three sub-packages:
 * <ul>
 *     <li><code>controller</code>, contains application
 *        controllers.</li>
 *     <li><code>model</code>, contains domain model classes
 *       such ass entities, value objects, repositories,
 *       services, etc.</li>
 *     <li><code>view</code>, contains classes that convert
 *       the model to/from a client-facing representation.
 *       By default a JSON format is used.</li>
 * </ul>
 */
@org.springframework.lang.NonNullApi
package it.laskaridis.payments.clearing;
